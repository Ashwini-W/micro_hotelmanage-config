test:
  property: Hello from github test prod yml repo

spring:
  application:
    name: config-prod
    #config default profile ie :8085/application/prod

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
